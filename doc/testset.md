What the set needs, minimum:
Known downfall, recovered — a creator with a well-documented controversy who visibly bounced back
Known downfall, never recovered — same, but the hostility never really faded
Controversial-by-design, never cancelled — your edge case from before; should produce zero change points despite naturally high generalized hate
Vanilla, never cancelled — simplest sanity check, near-total silence expected
Multiple controversies, one creator — tests whether ADR-003 correctly finds several distinct change points instead of blending them into one


